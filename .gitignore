# Local Netlify folder
.netlify

# Repair backups (generated by the admin repair system — never commit these)
data/backups/

# GitHub token saved via the admin panel — never commit this
.github-token.json

